import { ReactElement } from "react";

interface ArtistRestrictedPageProps {}

export default function ArtistRestrictedPage({}: ArtistRestrictedPageProps): ReactElement {
  return <span>You are not an artist</span>;
}
